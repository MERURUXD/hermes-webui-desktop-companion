# hwdc — 桌宠项目上下文（Hermes 项目记忆）

本仓库是 MEI 的 Windows 桌宠项目（desktop-pet / hwdc，Tauri v2 + Rust + WebView2 + WebUI 配套扩展）。Hermes 在此目录工作时自动加载本文件。

## 硬性约定

- **主开发分支 = `feat/tray-host`**。新实验一律开独立分支（`feat/<名字>`），做完经审阅再合；别在主干上堆实验。
- **仓库整洁红线**：hwdc 仓库只放桌宠相关的东西。与桌宠无关的安装/配置（Hermes 扩展、WebUI 配置等）一律不得落入仓库目录。历史教训：`HERMES_WEBUI_EXTENSION_DIR` 曾指向 `hwdc/extension`，导致 WebUI 面板装的扩展写进仓库被用户批评"污染工作仓库"；现已恢复 gallery 默认模式。`extension/` 只放桌宠配套的 WebUI 扩展本体。
- **多代理协作**：编码子代理与审阅子代理用不同模型（写审分离）；plan 先经审阅再实施。commit 小而独立（一个逻辑一个 commit），阶段性 squash 成功能级 commit。
- 与用户交流用中文；涉及事实/方案先上网核查再下结论。

## 构建/调试速查

完整构建循环、代码地图、已知坑：**tauri-desktop-app-windows 技能**（`references/hwdc-code-map.md`、`references/rust-windows-cargo-gotchas.md`、`references/taskwise-commit-and-plan-boundaries.md`）。编译产物/前端改动须重跑 `cargo build`（frontendDist 嵌入 exe，1s 完成=没生效）。

## 桌面配套扩展

`extension/` 是 WebUI 端扩展（桌面端 hwdc 与 Hermes WebUI 互动的界面层）。改动后验证走 WebUI `/api/extensions/status`；启用状态 = 文件在且不在 `extension-overrides.json` 的 disabled_extensions 里。
